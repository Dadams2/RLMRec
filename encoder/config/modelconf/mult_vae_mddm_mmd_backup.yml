optimizer:
  name: adam
  lr: 1.0e-3
  weight_decay: 0

train:
  epoch: 1000
  batch_size: 1024
  save_model: false
  loss: pairwise
  test_step: 3
  reproducible: true
  seed: 2024
  patience: 20

test:
  metrics: [recall, ndcg]
  k: [5, 10, 20]
  batch_size: 1024

data:
  type: general_cf
  name: amazon

model:
  name: mult_vae_mddm_mmd
  # general parameters here

  # MMD-specific parameters
  use_multi_scale: true  # Use multiple bandwidths for MMD
  mmd_weight: 15.0  # Scaling factor to make MMD comparable to KL
  mmd_bandwidth: 1.0  # Base bandwidth (only used if use_multi_scale: false)
  
  # dataset-specific parameters here
  dropout: 0.3
  reg_weight: 1.0e-6
  
  # for amazon - optimized for better performance
  amazon:
    dropout: 0.3
    beta: 0.4  # Lower beta gives more weight to MMD matching
    reg_weight: 1.0e-6
    use_multi_scale: true
    mmd_weight: 15.0  # Higher weight for stronger matching
    
  # for yelp
  yelp:
    dropout: 0.3
    beta: 0.4  
    reg_weight: 1.0e-6
    use_multi_scale: true
    mmd_weight: 12.0
    
  # for steam
  steam:
    dropout: 0.2
    beta: 0.4
    reg_weight: 1.0e-6
    use_multi_scale: true
    mmd_weight: 10.0


  embedding_size: 32